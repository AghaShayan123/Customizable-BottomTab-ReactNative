import React from 'react';
import {Text, View} from 'react-native';

export default Chat = () => {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: 'white',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>Chat</Text>
    </View>
  );
};
